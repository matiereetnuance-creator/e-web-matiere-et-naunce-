; Augmente les limites d'envoi pour les photos de réalisations (valeur par
; défaut de PHP : 2 Mo, trop restrictive pour des photos de smartphone).
; Fichier .user.ini : fonctionne quel que soit le mode d'exécution de PHP
; sur o2switch (contrairement à php_value dans .htaccess, qui peut
; provoquer une erreur 500 selon la configuration). Peut prendre jusqu'à
; quelques minutes pour être pris en compte (mise en cache de PHP-FPM).
;
; post_max_size est volontairement large : la galerie d'une réalisation
; accepte l'envoi de plusieurs photos en une fois (ex. 10 photos iPhone
; sélectionnées d'un coup), dont la taille cumulée dépasse largement celle
; d'un seul fichier.
;
; upload_max_filesize doit rester au-dessus de MN_RAW_CONVERT_MAX_BYTES
; (content.php, 45 Mo) : un vrai fichier Apple ProRAW de 30-40 Mo doit
; pouvoir atteindre le code du site pour qu'une conversion en JPEG lui
; soit proposée — en dessous de cette valeur, PHP le tronquerait avant
; même que le site ne le voie, rendant la proposition impossible.
upload_max_filesize = 50M
post_max_size = 200M
max_file_uploads = 30
